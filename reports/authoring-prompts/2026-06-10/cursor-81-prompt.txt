Author REGRESSION TESTS for ROAD-TO-8 item 8.1 (read reports/ROAD-TO-8.md): scrub failure must QUARANTINE the result, not persist it unscrubbed.

TODAY'S BEHAVIOR (src/rondo/dispatch.py ~816-834, the finalize path): when sanitize_task_result raises, the code warns + flags (RONDO-388) and then the UNSCRUBBED result proceeds to audit JSONL, result files, spool, and history — inverting STD-114 r006 (MUST scrub before writing). Also the except is narrow (TypeError, AttributeError): a RecursionError from _scrub_dict on deeply-nested parsed_result, or re.error from a bad custom pattern, either crashes finalize or sails through with raw data.

THE NEW CONTRACT (Mark's amended ruling — never-lose-data preserved via quarantine):
  (a) On ANY Exception from sanitize_task_result: the raw result is written to a QUARANTINE store — expect ~/.rondo/quarantine/ (honoring RONDO_TEST_DIR like audit/spool: <RONDO_TEST_DIR>/quarantine/), file born with restrictive perms (0o600), one JSON file per quarantined result with the error detail included.
  (b) The result is WITHHELD from the normal stores: audit outcome raw_output, result file, spool, history must NOT receive the unscrubbed payload. The TaskResult the caller gets back has raw_output/parsed_result REPLACED by a redaction stub (e.g. "[QUARANTINED: sanitize failed — see quarantine store]"), status preserved or marked, metrics["sanitize_failed"]=True, and a quarantine reference (path or id) in context_data.
  (c) The -WARNING- log remains (mentions quarantine + the file).
  (d) RecursionError and re.error are caught too (except Exception at this boundary).
  (e) Clean-path rail: normal results are untouched by all of this.

YOUR TESTS — tests/unit/test_sanitize_quarantine_cursor.py:
1. Monkeypatch rondo.dispatch.sanitize_task_result to raise TypeError; drive the finalize path (same harness as your test_sanitize_failopen_loud_cursor.py — reuse its approach) with RONDO_TEST_DIR set; assert: quarantine file EXISTS under <tmp>/quarantine/ containing the ORIGINAL raw_output + the error; returned result's raw_output is the stub (original secret-bearing text ABSENT); metrics flag; context_data quarantine reference; WARNING logged.
2. Same but raising RecursionError — must behave identically (kills the narrow-except).
3. Withholding proof: with spool enabled into RONDO_TEST_DIR, assert the spool file (if any) does NOT contain the original raw text; same for the audit jsonl if the harness wires an audit trail (keep this practical — assert on whichever stores the finalize path actually writes under your harness).
4. Perms: the quarantine file mode & 0o777 == 0o600 (POSIX).
5. Clean rail: sanitize succeeds → no quarantine dir/file created, no stub, no flag.
Tests 1-4 MUST FAIL today (no quarantine exists; raw persists; RecursionError uncaught).

NOTE: this supersedes 3 assertions in your test_sanitize_failopen_loud_cursor.py (which pinned persist-raw+flag). Do NOT edit that file — I will reconcile it during implementation with a clear commit note.

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file.
- Each test fn: -> None return hint + docstring. Two SPDX lines at top. Module docstring contains VER-001. Pure pytest + unittest.mock + caplog.
