Author REGRESSION TESTS for ROAD-TO-8 item 8.2 — the design YOU reviewed in reports/cursor-reviews/design-review-20260610-item82.md. All your recommendations were ACCEPTED except the plans-per-minute limiter (declined for now, scope control). Write the judge tests for the agreed contract.

THE AGREED CONTRACT (your design answer, accepted):
  (1) At the choke point (mcp_dispatch.py:872-873), advisory plans (engine inline|agent) get: audit INTENT recorded → plan dict mutated to carry the INTENT's dispatch_id → audit OUTCOME recorded immediately with status="advisory", exit_code 0 → THEN the plan JSON returned. AuditRecord gains an append-only `engine: str = ""` field (in to_dict too).
  (2) Sanitize on persist only: raw prompt goes INTO record_intent/record_outcome (module scrubs persisted copies); the RETURNED plan keeps the prompt verbatim.
  (3) Scope honesty on ALL THREE plan builders (dispatch_routing.py): inline+agent get guarantees_scope="advisory" with not_covered ⊇ ["budget","circuit_breaker","cost_tracking","result_audit","output_sanitization","idempotency"]; subprocess gets guarantees_scope="guarded". PLAN_SCHEMA_VERSION bumped to "3".
  (4) Estimate-gated budget at ISSUANCE: when the caller passes max_budget AND the token-estimate cost for the prompt+model exceeds it, the plan is REFUSED with a budget error envelope. No max_budget → no gate (declared, not faked).
  (5) Fail-open + loud: audit write failures never block the plan return; WARNING/ERROR logged.
  (6) Advisory dispatches NEVER touch the idempotency cache: no lookup hit can serve a cached guarded result to an advisory caller (your finding: the line-824 lookup keys on RAW execution before resolution — the fix moves/conditions the lookup so advisory skips it), and no store afterward. Two identical inline dispatches → fresh execution_token AND fresh dispatch_id each time.
  (7) reconcile_stuck_intents() finds 0 stuck records after an advisory dispatch (INTENT always paired with its advisory OUTCOME).

YOUR TESTS — tests/unit/test_advisory_path_machinery_cursor.py — write the 8 judges you yourself proposed in section (8) of the design review (audit completeness; sanitize both directions; scope non-ambiguity incl. subprocess="guarded"; dispatch_id correlation; fail-open loud; no-caching freshness; no reconcile false-positive; schema version "3" on advisory plans). Drive them through the PUBLIC entry (the MCP rondo_run path / its testable seam — find how existing tests in tests/unit/test_mcp_dispatch*.py drive inline plans with a session, and reuse that harness; RONDO_TEST_DIR is autouse so audit JSONL lands under tmp). Add a 9th for the estimate gate: max_budget tiny (e.g. 0.000001) + a paid-model agent plan → refused with a budget error envelope; no max_budget → plan issued.

Tests 1,3 (advisory fields),4,6,7,8,9 MUST FAIL today (bare `return json.dumps(engine)` at mcp_dispatch.py:872-873; schema version is "2"; no engine field on AuditRecord). State the RED list when done.

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file.
- Each test fn: -> None return hint + docstring. Two SPDX lines at top. Module docstring contains VER-001. Pure pytest + unittest.mock + caplog/monkeypatch. No live AI calls (advisory paths never dispatch anyway).
- Run the file and report which tests FAIL (RED) vs pass.
