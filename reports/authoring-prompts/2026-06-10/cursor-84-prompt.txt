Author REGRESSION TESTS for ROAD-TO-8 item 8.4 (read reports/ROAD-TO-8.md): _BudgetGate per-provider-class estimates. Your re-score finding R4.

TODAY'S DISEASE (src/rondo/parallel.py:230-309 _BudgetGate): ONE global _estimate float + ONE _have_sample bool. In a mixed round (free Claude max-auth tasks + paid gemini/openai tasks): a $0 free success calls settle(cost=0, ok=True) → _estimate=0.0, _have_sample=True → every PAID task thereafter admits with est=0.0 — the cap only stops them after money is already spent (reserve ≈ 0 means no admission pressure). Under-enforcement of IFS-101 r028 / STD-107 r018 / STD-101 r212 MUSTs in exactly the mixed-round case rondo ships as a feature (round files route per-task cloud models — RONDO-342).

THE NEW CONTRACT (per-class estimates, GLOBAL cap accounting):
  (a) try_admit/settle take a class key (provider class string, e.g. "gemini", "openai", "claude"; derivation from task model happens in _dispatch_worker — your tests may pass class keys directly to the gate).
  (b) Estimates and sampled-state are PER CLASS: a $0 free-class sample must NOT zero the estimate used to admit a paid-class task.
  (c) Cold-start probe is PER CLASS: first task of an UNSAMPLED class runs alone FOR THAT CLASS (peers of that class wait); tasks of already-sampled classes are unaffected by another class's probe.
  (d) Estimate update is MAX-KEEP, not last-write: within a gate's lifetime, a class's estimate never goes DOWN from a later cheaper paid sample (max of samples; the $0-success rule still applies only when ok=True and cost==0 → that class's estimate may be 0.0 — free is genuinely free).
  (e) Cap accounting (spent + reserved) stays GLOBAL across classes — one cap per round.
  (f) cap=None still disables everything.
  (g) Existing single-class behavior unchanged (reserve-then-settle, refund on failure, probe wait loop — pinned by tests/unit/test_budget_gate*.py already; don't duplicate, just don't break).

YOUR TESTS — tests/unit/test_budget_gate_per_class_cursor.py:
1. Mixed-round under-enforcement KILL: cap=1.0; class "free" settles $0 ok=True; class "paid" first task must be a PROBE (admitted alone) — and after its $0.60 settles, a second "paid" try_admit must be REFUSED (0.60 spent + 0.60 est > 1.0). On TODAY'S code (global estimate) the equivalent sequence admits paid tasks with est 0 — your test MUST FAIL today (api will differ: today's try_admit takes no class — write the test against the NEW signature; collection failure/TypeError on old code is an acceptable RED).
2. Per-class isolation: "gemini" sampled at $0.30 → "openai" first try_admit behaves as probe (est 0 reserve, runs alone for openai), not est-0.30 blind admit.
3. MAX-KEEP: paid class samples $0.50 then $0.05 → next admit reserves 0.50, not 0.05.
4. $0-success only zeroes ITS class: "free" $0 ok=True; "paid" $0.40 sampled; paid admits reserve 0.40, free admits reserve 0.0.
5. Global cap shared: cap=1.0, "a" spends 0.70, "b" sampled at 0.40 → b's next admit refused (0.70+0.40>1.0).
6. cap=None: try_admit(any class) returns 0.0 always, settle no-ops.
7. Probe-wait per class (threaded): two "paid" tasks start with no sample — exactly one admits immediately, the second blocks until settle; a concurrent "free"-class task with an existing free sample admits WITHOUT waiting on paid's probe. Use threading.Event + timeouts <2s, no sleeps>0.2s.

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file. The tests define the NEW gate API: try_admit(class_key: str) -> float | None; settle(class_key: str, reserved: float, cost: float, ok: bool = True). If you disagree with that signature, write the tests against the signature you'd choose and SAY SO at the top of your final message.
- Each test fn: -> None return hint + docstring. Two SPDX lines at top. Module docstring contains VER-001. Pure pytest + threading; no live dispatch, no sleeps as synchronization.
- Run the file; report the RED list (expect: all but cap=None fail on today's code — signature mismatch REDs are fine).
