Author REGRESSION TESTS for ROAD-TO-8 item 8.6 (read reports/ROAD-TO-8.md): reconcile must hold the flock ON THE AUDIT JSONL itself — STD-110 r016 literal. Your re-score finding R6.

TODAY'S HOLE (verified): src/rondo/audit.py — _append_jsonl (line ~632) takes LOCK_EX on the AUDIT FILE for each append, but reconcile (_reconcile_cross_process, line ~741) locks only a SIDECAR (.reconcile.lock). So an appender and a reconciler never contend: a real OUTCOME can land between reconcile's scan and its synthetic-stuck write → the same dispatch_id ends up with BOTH a real OUTCOME and a synthetic status="stuck" OUTCOME (double-outcome, the exact r016 read-modify-write race RONDO-359 was supposed to close cross-process).

THE NEW CONTRACT:
  (a) During reconcile's scan+write critical section, the implementation ALSO holds LOCK_EX on the audit JSONL file itself (the same file _append_jsonl flocks) — so concurrent appends serialize against reconcile. The sidecar may remain for reconciler-vs-reconciler mutual exclusion (skip-if-peer-running semantics unchanged).
  (b) Proof-of-exclusion: while reconcile is INSIDE its critical section, a second process attempting LOCK_EX|LOCK_NB on the audit JSONL must get EWOULDBLOCK.
  (c) Degradation rails unchanged: Windows/no-fcntl + NFS fallbacks still warn-and-proceed (pinned by existing tests/unit/test_reconcile_*; don't break).
  (d) Behavior unchanged otherwise: stuck INTENTs still get synthetic OUTCOMEs; paired INTENTs untouched.

YOUR TESTS — tests/unit/test_reconcile_audit_flock_cursor.py:
1. EXCLUSION KILL: arrange a reconcile that is mid-critical-section (monkeypatch the scan step — e.g. the function reconcile uses to read/scan the JSONL — to signal an Event and block until released). From a REAL second process (subprocess, sys.executable -c), attempt fcntl.flock(audit_jsonl_fd, LOCK_EX|LOCK_NB) while reconcile is blocked inside: must FAIL with EWOULDBLOCK. Today it SUCCEEDS (no lock on the jsonl) — MUST FAIL today. Use threads + events, hard timeout <10s.
2. Double-outcome race pin (deterministic interleave): monkeypatch the scan to (i) read the file, (ii) BEFORE writing synthetic outcomes, append a REAL outcome line for one of the stuck dispatch_ids via the public record_outcome from the same process is fine — the point is the implementation must either see it (because the lock forces the append to wait, then reconcile re-reads/sees consistent state) or the lock prevents the interleave. Practical pin: with the audit-file lock held by reconcile, an _append_jsonl call in a THREAD must BLOCK until reconcile finishes (measure: thread doesn't complete while reconcile is held open, completes after). MUST FAIL today (append succeeds immediately).
3. Clean rail: a normal reconcile (no contention) still writes synthetic stuck OUTCOMEs for old unpaired INTENTs (stuck_after_sec=0) and returns the right count; paired INTENTs yield 0.
4. Sidecar semantics unchanged: with the sidecar lock held by a fake peer (open + LOCK_EX from a thread or subprocess), reconcile_stuck_intents returns 0 (skip-as-peer-running).

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file.
- Each test fn: -> None return hint + docstring. Two SPDX lines. Module docstring contains VER-001. POSIX-skip where fcntl assertions; total runtime <15s; no sleeps as synchronization (Events + timeouts).
- Run the file; report the RED list (expect 1,2 RED; 3,4 pass).
