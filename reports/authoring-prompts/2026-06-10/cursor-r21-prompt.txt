Author REGRESSION TESTS for ROAD-TO-8 item R2-1 (read reports/ROAD-TO-8.md, ROUND 2): the in-memory idempotency cache is unbounded on the long-lived MCP server. Your re-score finding #1 (review-20260610-184904.md).

TODAY'S DISEASE (src/rondo/idempotency.py): module-level `_cache: dict[str, tuple[Any, float]]` — `cache_result` and the file→memory promote add one entry holding the FULL result payload per unique prompt key; the ONLY eviction is same-key re-lookup after TTL. Unique prompts (every distinct Cursor prompt = new SHA-256 key) grow it forever. The in-memory twin of the disk/lock leaks RONDO-369/396 fixed.

THE NEW CONTRACT:
  (a) BOUNDED: the in-memory cache never exceeds a max entry count (module constant, e.g. _MAX_MEMORY_ENTRIES = 512; pick what the implementation exposes — name it in your tests and say so). Inserting past the bound evicts the OLDEST entries (by cached_at timestamp) — never fails the insert.
  (b) EXPIRED SWEEP: entries past TTL are removed opportunistically (at minimum during inserts once the cache is large; a dedicated sweep function is acceptable — pin the OBSERVABLE: after inserting fresh entries beyond the bound with some entries expired, expired ones are gone WITHOUT their keys ever being re-looked-up).
  (c) Correctness preserved: a bounded-out (evicted) key is NOT a correctness loss — the JSONL file layer still serves it (get_cached_result falls through to the file scan and re-promotes). Pin that: evict a key from memory via the bound, then get_cached_result(key) still returns the value (from disk).
  (d) Hot-key behavior: recently WRITTEN keys survive eviction preferentially (oldest-out). A fresh insert is never evicted by its own insertion.
  (e) Thread-safety: concurrent cache_result calls during eviction don't corrupt (no exceptions, final size <= bound). Use a handful of threads, bounded joins, no sleeps as sync.

YOUR TESTS — tests/unit/test_idempotency_memory_bound_cursor.py:
1. KILL: insert (bound + 50) unique keys via the public cache_result; assert the module-level in-memory dict size <= the bound. MUST FAIL today (grows to bound+50).
2. Oldest-out: fill to bound with controllable cached_at ordering (freezegun is NOT in deps — drive age via the time seam the module uses, monkeypatch time.time or insert order), insert one more, assert the OLDEST key is the one gone from memory and the NEWEST survives.
3. Disk fallback: a memory-evicted key still resolves via get_cached_result (the JSONL has it; RONDO_TEST_DIR isolation is autouse).
4. Expired sweep: with TTL-expired entries present, inserts (or the exposed sweep) remove them without re-lookup. MUST FAIL today.
5. Threaded: N threads x M inserts concurrently; afterwards len(memory dict) <= bound and no exception escaped.

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file.
- Each test fn: -> None return hint + docstring. Two SPDX lines. Module docstring contains VER-001. Use the gitleaks-safe fixture style; access the private _cache dict for size assertions is acceptable (it IS the subject) — note it.
- Run the file; report the RED list (expect 1, 2, 4 RED; 3 maybe-pass; 5 RED on size).
