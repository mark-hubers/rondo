Author REGRESSION TESTS for ROAD-TO-8 item R2-2 (read reports/ROAD-TO-8.md, ROUND 2): the scrub set is incomplete — secret-bearing fields reach spool/history RAW. Your re-score finding #2 (review-20260610-184904.md). Also fold in finding #8 (archive JSONL born at umask) — same STD-110/114 family, one-line fix, pin it here.

TODAY'S DISEASE:
- src/rondo/sanitize.py sanitize_task_result scrubs exactly 4 fields: prompt_sent, raw_output, stderr, parsed_result. But _finalize_dispatch persists asdict(result) to SPOOL and HISTORY — error_message, context_data, command_sent (verify which of these exist on TaskResult in src/rondo/engine.py and scope to the real ones) go RAW. error_message routinely carries provider/subprocess error text (which can embed keys — the URLError ?key=SECRET class). STD-114 r006 MUST: scrub before writing result/spool/reports.
- src/rondo/audit.py rotation: archive_path.open("a") creates the rotated archive at umask — the one 8.3 twin still wide (your finding #8).

THE NEW CONTRACT:
  (a) sanitize_task_result's scrub set covers ALL secret-capable string/dict fields that finalize persists: at minimum error_message and context_data (dict — string values scrubbed recursively, the _scrub_dict machinery exists) plus command_sent if TaskResult has it. Clean values pass through unchanged.
  (b) Withholding proof at the STORE level: plant a distinctive fake secret (use the AKIAIOSFODNN7EXAMPLE family ONLY — gitleaks + the api_key= conventions lock; name constants like _AWS_EXAMPLE_KEY, never *api_key* =) in error_message AND in a context_data value; run the finalize path with spool enabled under RONDO_TEST_DIR; assert NO file under spool/ or history/ contains the secret, and the scrubbed marker ([REDACTED) is present where the secret was.
  (c) Audit archive: force a rotation (config.max_jsonl_bytes tiny) under umask 0o000; the rotated archive file's mode & 0o777 == 0o600. POSIX-skip. MUST FAIL today.
  (d) Rails: existing 4-field scrubbing unchanged (a secret in raw_output still scrubbed — quick rail); sanitize report counts still sane; clean results untouched (no [REDACTED in clean text).

YOUR TESTS — tests/unit/test_scrub_set_complete_cursor.py:
1. error_message scrubbed by sanitize_task_result (unit level). MUST FAIL today.
2. context_data string values scrubbed recursively (nested dict). MUST FAIL today.
3. Store-level withholding: secret in error_message + context_data → absent from every spool/ + history/ file after finalize. MUST FAIL today.
4. Archive perms 0o600 under umask 0 after forced rotation. MUST FAIL today.
5. Rails: raw_output scrub unchanged; clean-path untouched.

HARD CONSTRAINTS:
- Do NOT modify ANY file under src/. Tests only. One new file.
- Each test fn: -> None return hint + docstring. Two SPDX lines. Module docstring contains VER-001. Reuse the finalize harness style from tests/unit/test_sanitize_quarantine_cursor.py. Restore umask in a fixture/finally.
- Run the file; report the RED list (expect 1-4 RED, 5 pass).
