Author REGRESSION TESTS for ROAD-TO-8 item R2-4 (read reports/ROAD-TO-8.md, ROUND 2): advisory OUTCOME vs STD-113 — req 021 MUST + the §8 state vocabulary. Your re-score finding #4 (review-20260610-184904.md).

TODAY'S DISEASE:
- STD-113 req 021 (MUST): every non-`done` OUTCOME persists error_message. The issued-advisory OUTCOME passes error_message="" (src/rondo/mcp_dispatch.py _audit_advisory_plan), explanation lives only in blocked_reason.
- STD-113 §8 still declares "two states: INTENT and COMPLETE" while code emits done/error/blocked/skipped/stuck/advisory/refused — spec-vs-code drift.

THE NEW CONTRACT (both sides fixed):
  (a) CODE: issued-advisory OUTCOMEs persist a non-empty error_message-style explanation satisfying the req-021 reading (e.g. "advisory plan returned to host — delegated execution, result not observed"); refused OUTCOMEs already carry the refusal text — pin both non-empty in the JSONL record.
  (b) SPEC: specs/Rondo-STD-113-*.md §8 amended to the de-facto OPEN status vocabulary: INTENT plus a terminal-status family explicitly listing done/error/blocked/skipped/stuck/advisory/refused, with a note that advisory/refused are delegated-execution outcomes (RONDO-394/399). Your tests pin the SPEC TEXT TOO: read the spec file and assert "advisory" and "refused" appear in §8/the states section, and the "two states" claim is gone.
  (c) Rails: normal done OUTCOMEs unchanged (error_message still optional/empty on success); reconcile still pairs advisory INTENT+OUTCOME (0 stuck).

YOUR TESTS — tests/unit/test_advisory_outcome_std113_cursor.py:
Reuse the rondo_run_file + RONDO_TEST_DIR harness from tests/unit/test_advisory_path_machinery_cursor.py.
1. Issued advisory OUTCOME record has non-empty error_message in the audit JSONL. MUST FAIL today.
2. Refused OUTCOME (tiny max_budget + agent/sonnet) has non-empty error_message AND error_code ERR_BUDGET_EXCEEDED. (May already pass — verify and report.)
3. Spec pin: the STD-113 spec file's states/§8 section mentions advisory AND refused and no longer claims exactly two states. MUST FAIL today.
4. Rail: a normal done OUTCOME (drive any guarded finalize path or build the record via record_outcome directly) still allows empty error_message.
5. Rail: reconcile_stuck_intents()==0 after an advisory dispatch (unchanged).

You MAY edit the spec file? NO — do NOT modify ANY file (src/ OR specs/). Tests only; the spec amendment is Claude's implementation step. Your test 3 simply reads the spec and is RED until Claude amends it.

HARD CONSTRAINTS: one new test file; -> None hints + docstrings; two SPDX lines; VER-001 in module docstring. Run it; report the RED list (expect 1,3 RED; 2 verify; 4,5 pass).
